# CI 分诊定时任务:逻辑归并进 skill + prompt 瘦身

## 目标

把当前由 prompt 硬编码的 3/5/6/8 步逻辑归并进 `vllm-omni-CI-Inspection` skill,过滤逻辑从"转发时过滤"挪到"汇总表本身就是过滤后结果",prompt 瘦身到只剩"分析 xxx CI 失败 + 转发告警群"。

## 现状(已核实)

SKILL.md(1060 行)已内置全部相关逻辑,只是 3 处需要改:

| 步骤 | skill 现状 | 需要改? |
|---|---|---|
| 3 数据拉取+过滤 | 313-506 行,完整 | 否 |
| 5 汇总表输出 | 825-853 行,逐行格式 | 是:汇总表=过滤后结果 |
| 6 CSV 追加 | 855-866 行,硬编码路径 | 是:询问+默认当前目录 |
| 7 告警转发过滤 | 868-880 行,转发时过滤 | 是:过滤挪到汇总表 |
| 8 提issue链接 | 694-809 + 890-1043 行 | 否(已正确) |

## A. SKILL.md 改造(3 处)

### A1. 汇总表=过滤后结果(改 5+7 步,825-880 行)

合并 `### Summary Output Format` 和 `### Alert Group Forwarding`:
- 汇总表生成时即按 CI 类型过滤:READY/NPU 剔除🔴PR引入;MERGE 不过滤
- 告警群转发=直接转发汇总表,不再二次过滤
- 全 PR引入(过滤后空)→ 静默不发

### A2. CSV 路径询问+默认(改 6 步,855-866 行)

CSV 目录优先级:env `CI_TRIAGE_DATA_DIR` > 默认 `<cwd>/ci_triage_data/`(cron 无人值守,直接用默认,不强制询问)

### A3. 静默模式(886-888 行,保留强化)

## B. Prompt 瘦身(重建 3 个 cron)

prompt 只剩:URL + CI类型 + 分支 + 触发点(CST) + token + "执行完整分诊流程并转发告警群"

## C. 重建任务 + D. 验证

删旧3个 → 新prompt重建 → exec MERGE 验证
