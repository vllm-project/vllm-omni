steps:
  # NPU Simple Test - Quick validation that NPU test environment works
  # Once this passes, replace with bootstrap-npu-omni.sh for full CI
  - label: "Upload NPU Simple Test"
    key: upload-npu-simple-test
    commands:
      - buildkite-agent pipeline upload .buildkite/pipeline-npu-simple-test.yaml
    agents:
      queue: "cpu_queue_premerge"
