env:
  VLLM_WORKER_MULTIPROC_METHOD: spawn
  VLLM_USE_FLASHINFER_MOE_FP16: 0
  HF_HUB_DOWNLOAD_TIMEOUT: 300
  HF_HUB_ETAG_TIMEOUT: 60

steps:
  - label: "Unified tests with merged coverage"
    depends_on: upload-ready-pipeline
    commands:
      - |
        pytest -v -s -m 'core_model and cpu' \
        --cov=vllm_omni --cov-report=

        pytest -v -s -m 'core_model and cuda and L4 and not distributed_cuda' \
        --cov=vllm_omni --cov-append --cov-report= \
        --ignore=tests/e2e --ignore=tests/engine/test_async_omni_engine_abort.py

        pytest -v -s -m 'core_model and cuda and L4 and distributed_cuda' \
        --cov=vllm_omni --cov-append --cov-report=

        coverage report -m --skip-covered
        coverage html --skip-covered
        coverage xml --skip-covered
    agents:
      queue: "gpu_4_queue"
    plugins:
      - docker#v5.2.0:
          image: public.ecr.aws/q9t5s3a7/vllm-ci-test-repo:$BUILDKITE_COMMIT
          always-pull: true
          propagate-environment: true
          environment:
            - "HF_HOME=/fsx/hf_cache"
            - "HF_TOKEN"
          volumes:
            - "/fsx/hf_cache:/fsx/hf_cache"
